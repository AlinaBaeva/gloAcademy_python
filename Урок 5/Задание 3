# определить, равна ли сумма первых 3х слагаемых вторым 3м у любого 6ти значного числа
num = int(input())
sum_num_1 = (num // 1000) % 10 + ((num // 1000) // 10) % 10 + (num // 1000) // 100
sum_num_2 = (num % 1000) % 10 + ((num % 1000) // 10) % 10 + (num % 1000) // 100

if sum_num_1 == sum_num_2:
    print('Билет', num, 'счастливый')
else:
   print('Билет', num, 'НЕсчастливый')

