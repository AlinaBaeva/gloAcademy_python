# Определите является ли данное число замечательным.
a = int(input())
number = a
total = 0
while number != 0:
    last_digit = number % 10
    total += last_digit
    number = number // 10
    if a % total == 0:
        print('YES')
    else:
        print('NO')
